"use client";

import { StaticPage } from "@/components/StaticPage";

/** Refund policy placeholder. */
export default function RefundPolicyPage() {
  return (
    <StaticPage title="Refund & Return Policy" breadcrumb="Refund and Return Policy">
      Please check the product during delivery. Returns are accepted on the
      spot with delivery charges only. Full policy details will be listed here.
    </StaticPage>
  );
}
